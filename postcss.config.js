// Tailwind CSS v4 is handled by the @tailwindcss/vite plugin in vite.config.js
// PostCSS only needs autoprefixer for vendor prefixes
export default {
    plugins: {
        autoprefixer: {},
    },
};
